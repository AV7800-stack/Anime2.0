# Anime 2.0

Project lives in: `anime-video-generator/`

Run:
1. `cd "anime-video-generator"`
2. Copy `.env.example` to `.env`
3. `pip install -r requirements.txt`
4. Make sure `ffmpeg` is installed and on PATH
5. `python run.py`

Web UI: `http://localhost:8000`

