import os

folders = [
    "app",
    "app/auth",
    "app/pipeline",
    "app/services",
    "app/templates",
    "app/static",
    "output",
    "data",
    "models",
    "logs",
]

files = {
    "requirements.txt": """fastapi\nuvicorn\njinja2\npython-multipart\nsqlalchemy\npython-jose[cryptography]\npasslib[bcrypt]\npython-dotenv\nopenai\ndiffusers\ntransformers\naccelerate\nmoviepy\nedge-tts\npillow""",
    ".env.example": """OPENAI_API_KEY=your_openai_api_key_here\nELEVENLABS_API_KEY=your_elevenlabs_api_key_here\nSECRET_KEY=supersecretkey123\nDATABASE_URL=sqlite:///./data/anime.db\nLOW_END_MODE=true\nFIREBASE_API_KEY=optional_firebase_key""",
    "run.py": """import uvicorn\nimport os\n\nif __name__ == "__main__":\n    os.makedirs("output", exist_ok=True)\n    os.makedirs("data", exist_ok=True)\n    uvicorn.run("app.main:app", host="127.0.0.1", port=8000, reload=True)""",
    "app/__init__.py": "",
    "app/pipeline/__init__.py": "",
    "app/database.py": """from sqlalchemy import create_engine\nfrom sqlalchemy.orm import declarative_base, sessionmaker\nimport os\n\nSQLALCHEMY_DATABASE_URL = "sqlite:///./data/anime.db"\nengine = create_engine(SQLALCHEMY_DATABASE_URL, connect_args={"check_same_thread": False})\nSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)\nBase = declarative_base()""",
    "app/models.py": """from sqlalchemy import Column, Integer, String, Boolean\nfrom .database import Base\n\nclass User(Base):\n    __tablename__ = "users"\n    id = Column(Integer, primary_key=True, index=True)\n    username = Column(String, unique=True, index=True)\n    hashed_password = Column(String)""",
    "app/auth.py": """from datetime import datetime, timedelta\nfrom jose import JWTError, jwt\nfrom passlib.context import CryptContext\nfrom fastapi import Depends, HTTPException, status, Request\nfrom sqlalchemy.orm import Session\nfrom .database import SessionLocal\nfrom .models import User\n\nSECRET_KEY = "supersecretkey123" # In production, use .env\nALGORITHM = "HS256"\n\npwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")\n\ndef get_password_hash(password):\n    return pwd_context.hash(password)\n\ndef verify_password(plain_password, hashed_password):\n    return pwd_context.verify(plain_password, hashed_password)\n\ndef create_access_token(data: dict):\n    to_encode = data.copy()\n    expire = datetime.utcnow() + timedelta(minutes=60)\n    to_encode.update({"exp": expire})\n    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)\n\ndef get_db():\n    db = SessionLocal()\n    try:\n        yield db\n    finally:\n        db.close()\n\nasync def get_current_user(request: Request, db: Session = Depends(get_db)):\n    token = request.cookies.get("access_token")\n    if not token:\n        raise HTTPException(status_code=401, detail="Not authenticated")\n    try:\n        token = token.replace("Bearer ", "")\n        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])\n        username: str = payload.get("sub")\n        if username is None:\n            raise HTTPException(status_code=401)\n    except JWTError:\n        raise HTTPException(status_code=401)\n    user = db.query(User).filter(User.username == username).first()\n    if user is None:\n        raise HTTPException(status_code=401)\n    return user""",
    "app/main.py": """from fastapi import FastAPI, Request, Form, Depends, BackgroundTasks\nfrom fastapi.templating import Jinja2Templates\nfrom fastapi.staticfiles import StaticFiles\nfrom fastapi.responses import HTMLResponse, RedirectResponse\nimport os\nfrom .database import engine, Base, SessionLocal\nfrom .models import User\nfrom .auth import get_password_hash, verify_password, create_access_token, get_current_user\nfrom sqlalchemy.orm import Session\nfrom .pipeline.orchestrator import run_pipeline\n\nBase.metadata.create_all(bind=engine)\n\napp = FastAPI(title="Anime Video Generator AI")\n\napp.mount("/static", StaticFiles(directory="app/static"), name="static")\napp.mount("/output", StaticFiles(directory="output"), name="output")\ntemplates = Jinja2Templates(directory="app/templates")\n\ndef get_db():\n    db = SessionLocal()\n    try:\n        yield db\n    finally:\n        db.close()\n\n@app.get("/", response_class=HTMLResponse)\nasync def read_root(request: Request):\n    return templates.TemplateResponse("login.html", {"request": request})\n\n@app.post("/login")\nasync def login(request: Request, username: str = Form(None), password: str = Form(None), phone: str = Form(None), db: Session = Depends(get_db)):\n    # Phone auth stub\n    if phone:\n        return templates.TemplateResponse("login.html", {"request": request, "message": f"OTP sent to {phone} (Stub)"})\n    user = db.query(User).filter(User.username == username).first()\n    if not user or not verify_password(password, user.hashed_password):\n        return templates.TemplateResponse("login.html", {"request": request, "error": "Invalid credentials"})\n    response = RedirectResponse(url="/dashboard", status_code=302)\n    access_token = create_access_token(data={"sub": user.username})\n    response.set_cookie(key="access_token", value=f"Bearer {access_token}", httponly=True)\n    return response\n\n@app.post("/signup")\nasync def signup(request: Request, username: str = Form(...), password: str = Form(...), db: Session = Depends(get_db)):\n    if db.query(User).filter(User.username == username).first():\n         return templates.TemplateResponse("login.html", {"request": request, "error": "Username exists", "is_signup": True})\n    new_user = User(username=username, hashed_password=get_password_hash(password))\n    db.add(new_user)\n    db.commit()\n    return templates.TemplateResponse("login.html", {"request": request, "message": "Signup successful. Please login."})\n\n@app.get("/dashboard", response_class=HTMLResponse)\nasync def dashboard(request: Request, user: User = Depends(get_current_user)):\n    return templates.TemplateResponse("dashboard.html", {"request": request, "user": user})\n\n@app.post("/api/generate")\nasync def generate_video(background_tasks: BackgroundTasks, user: User = Depends(get_current_user), prompt: str = Form(...), style: str = Form("cinematic"), low_end_mode: bool = Form(False)):\n    job_id = f"job_{os.urandom(4).hex()}"\n    background_tasks.add_task(run_pipeline, job_id, prompt, style, low_end_mode)\n    return {"status": "success", "job_id": job_id, "message": "Generation started"}\n""",
    "app/pipeline/orchestrator.py": """import os\nimport json\nimport time\nfrom .story import generate_story\nfrom .image import generate_images\nfrom .audio import generate_audio\nfrom .video import assemble_video\n\ndef run_pipeline(job_id: str, prompt: str, style: str, low_end_mode: bool):\n    job_dir = f"output/{job_id}"\n    os.makedirs(job_dir, exist_ok=True)\n    print(f"[{job_id}] Starting pipeline for prompt: {prompt}")\n    \n    try:\n        # 1. Story\n        story = generate_story(prompt)\n        with open(f"{job_dir}/story.json", "w") as f:\n            json.dump(story, f, indent=4)\n            \n        # 2. Images\n        image_paths = generate_images(story, style, job_dir, low_end_mode)\n        \n        # 3. Audio\n        audio_paths = generate_audio(story, job_dir)\n        \n        # 4. Video\n        final_video = assemble_video(image_paths, audio_paths, job_dir)\n        print(f"[{job_id}] Finished! Video saved at {final_video}")\n    except Exception as e:\n        print(f"[{job_id}] Error during pipeline: {e}")\n        with open(f"{job_dir}/error.log", "w") as f:\n            f.write(str(e))\n""",
    "app/pipeline/story.py": """import json\nimport os\n# Use OpenAI if key exists, else fallback to a basic mock\n\ndef generate_story(prompt: str):\n    print(f"Generating story for: {prompt}")\n    # Mocking for immediate runnability without API keys\n    return {\n        "title": "Generated Story",\n        "scenes": [\n            {\n                "scene_num": 1,\n                "description": "Opening establishing shot based on the prompt.",\n                "narration": f"It begins here: {prompt}",\n                "prompt": f"{prompt}, highly detailed, anime style, cinematic lighting"\n            },\n            {\n                "scene_num": 2,\n                "description": "Close up action shot.",\n                "narration": "The journey continues.",\n                "prompt": "close up, action pose, dynamic angle, anime style, 8k"\n            }\n        ]\n    }\n""",
    "app/pipeline/image.py": """import os\nfrom PIL import Image, ImageDraw, ImageFont\n# In production, import diffusers here\n\ndef generate_images(story: dict, style: str, output_dir: str, low_end_mode: bool):\n    paths = []\n    for scene in story["scenes"]:\n        img_path = f"{output_dir}/scene_{scene['scene_num']}.png"\n        # MOCK IMPLEMENTATION: Create dummy images to avoid heavy downloads initially\n        # Real implementation would load StableDiffusionPipeline here using diffusers\n        width, height = (800, 450) if low_end_mode else (1920, 1080)\n        img = Image.new('RGB', (width, height), color = (20, 20, 30))\n        d = ImageDraw.Draw(img)\n        d.text((50, height//2), f"Scene {scene['scene_num']}\\nStyle: {style}\\nPrompt: {scene['prompt'][:40]}...", fill=(255,200,0))\n        img.save(img_path)\n        paths.append(img_path)\n        print(f"Generated image: {img_path}")\n    return paths\n""",
    "app/pipeline/audio.py": """import os\n# import edge_tts\n\ndef generate_audio(story: dict, output_dir: str):\n    paths = []\n    for scene in story["scenes"]:\n        audio_path = f"{output_dir}/scene_{scene['scene_num']}.mp3"\n        # MOCK IMPLEMENTATION\n        # Real implementation would use edge-tts or ElevenLabs\n        # For now, returning None to let video assembler know to skip audio or use silence\n        paths.append(None)\n    return paths\n""",
    "app/pipeline/video.py": """import os\nfrom moviepy.editor import ImageClip, concatenate_videoclips\n\ndef assemble_video(image_paths: list, audio_paths: list, output_dir: str):\n    clips = []\n    for img_path in image_paths:\n        # 3 seconds per scene for basic implementation\n        clip = ImageClip(img_path).set_duration(3)\n        clips.append(clip)\n        \n    final_clip = concatenate_videoclips(clips, method="compose")\n    output_path = f"{output_dir}/final_video.mp4"\n    # Write video. fps=24 is standard for anime.\n    final_clip.write_videofile(output_path, fps=24, codec="libx264")\n    return output_path\n""",
    "app/templates/login.html": """<!DOCTYPE html>\n<html lang="en">\n<head>\n    <meta charset="UTF-8">\n    <title>Anime Video Generator AI - Login</title>\n    <link rel="stylesheet" href="/static/style.css">\n</head>\n<body>\n    <div class="container">\n        <h1>Anime Video Generator</h1>\n        {% if error %}<p class="error">{{ error }}</p>{% endif %}\n        {% if message %}<p class="success">{{ message }}</p>{% endif %}\n        <div class="tabs">\n            <button onclick="document.getElementById('emailForm').style.display='block'; document.getElementById('phoneForm').style.display='none';">Email</button>\n            <button onclick="document.getElementById('phoneForm').style.display='block'; document.getElementById('emailForm').style.display='none';">Phone (Firebase)</button>\n        </div>\n        <form id="emailForm" action="{% if request.query_params.get('is_signup') %}/signup{% else %}/login{% endif %}" method="post">\n            <input type="text" name="username" placeholder="Username" required>\n            <input type="password" name="password" placeholder="Password" required>\n            <button type="submit" class="btn-primary">{% if request.query_params.get('is_signup') %}Sign Up{% else %}Login{% endif %}</button>\n        </form>\n        <form id="phoneForm" action="/login" method="post" style="display:none;">\n            <input type="text" name="phone" placeholder="+1234567890" required>\n            <button type="submit" class="btn-secondary">Send OTP</button>\n            <p class="small">Uses Firebase Auth (Requires config)</p>\n        </form>\n        <p class="toggle-link">{% if request.query_params.get('is_signup') %}<a href="/">Back to Login</a>{% else %}<a href="/?is_signup=1">Create Account</a>{% endif %}</p>\n    </div>\n</body>\n</html>""",
    "app/templates/dashboard.html": """<!DOCTYPE html>\n<html lang="en">\n<head>\n    <meta charset="UTF-8">\n    <title>Dashboard - Anime Generator</title>\n    <link rel="stylesheet" href="/static/style.css">\n</head>\n<body>\n    <div class="container dash-container">\n        <header class="dash-header">\n            <h2>Welcome, {{ user.username }}</h2>\n            <button onclick="document.cookie='access_token=; Max-Age=0; path=/'; window.location.href='/';" class="btn-danger">Logout</button>\n        </header>\n        <main>\n            <div class="glass-card">\n                <h3>Create New Anime</h3>\n                <form id="generateForm">\n                    <textarea name="prompt" placeholder="Enter your anime story idea... (e.g., 'A cyberpunk samurai fighting in neo-Tokyo')" rows="4" required></textarea>\n                    <div class="settings-grid">\n                        <select name="style">\n                            <option value="cinematic">Cinematic Anime</option>\n                            <option value="ghibli">Ghibli Style</option>\n                            <option value="shonen">Action Shonen</option>\n                            <option value="dark">Dark Fantasy</option>\n                        </select>\n                        <select name="voice">\n                            <option value="narrator1">Deep Narrator</option>\n                            <option value="anime_girl">Anime Female</option>\n                        </select>\n                    </div>\n                    <label class="toggle-label"><input type="checkbox" name="low_end_mode" value="true" checked> Low-End Mode (Faster, uses CPU, Mock generation for testing)</label>\n                    <button type="submit" class="btn-primary btn-large">Generate Anime Video</button>\n                </form>\n            </div>\n            <div id="statusArea" class="status-box hidden"></div>\n            <div class="history-section">\n                <h3>Recent Outputs</h3>\n                <p class="small">Generated videos will appear in the /output folder.</p>\n            </div>\n        </main>\n    </div>\n    <script>\n        document.getElementById('generateForm').onsubmit = async (e) => {\n            e.preventDefault();\n            const formData = new FormData(e.target);\n            const statusArea = document.getElementById('statusArea');\n            statusArea.classList.remove('hidden');\n            statusArea.innerText = "Initializing AI Pipeline...";\n            try {\n                const res = await fetch('/api/generate', { method: 'POST', body: formData });\n                const data = await res.json();\n                statusArea.innerHTML = `Job <b>${data.job_id}</b> started! <br>Check your console and the <code>/output/${data.job_id}</code> folder.`;\n            } catch (err) {\n                statusArea.innerText = "Error starting job.";\n            }\n        };\n    </script>\n</body>\n</html>""",
    "app/static/style.css": """body {\n    background-color: #0f172a;\n    color: #f8fafc;\n    font-family: 'Inter', -apple-system, sans-serif;\n    display: flex;\n    justify-content: center;\n    align-items: center;\n    min-height: 100vh;\n    margin: 0;\n    background-image: radial-gradient(circle at top right, #1e1b4b, #0f172a);\n}\n.container {\n    background: rgba(30, 41, 59, 0.7);\n    backdrop-filter: blur(10px);\n    padding: 2.5rem;\n    border-radius: 16px;\n    border: 1px solid rgba(255, 255, 255, 0.1);\n    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);\n    text-align: center;\n    width: 350px;\n}\n.dash-container { width: 800px; align-items: flex-start; justify-content: flex-start; }\n.dash-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; border-bottom: 1px solid #334155; padding-bottom: 10px; }\ninput, textarea, select {\n    width: 100%;\n    padding: 12px;\n    margin: 10px 0;\n    background: rgba(15, 23, 42, 0.8);\n    border: 1px solid #334155;\n    color: white;\n    border-radius: 8px;\n    box-sizing: border-box;\n    font-size: 14px;\n}\ninput:focus, textarea:focus { outline: none; border-color: #8b5cf6; box-shadow: 0 0 0 2px rgba(139, 92, 246, 0.3); }\n.tabs { display: flex; gap: 10px; margin-bottom: 15px; }\n.tabs button { flex: 1; padding: 8px; background: #334155; font-size: 12px; }\nbutton {\n    width: 100%;\n    padding: 12px;\n    border: none;\n    border-radius: 8px;\n    cursor: pointer;\n    font-weight: 600;\n    transition: all 0.2s;\n}\n.btn-primary { background: linear-gradient(135deg, #8b5cf6, #6d28d9); color: white; }\n.btn-primary:hover { opacity: 0.9; transform: translateY(-1px); }\n.btn-secondary { background: #334155; color: white; }\n.btn-danger { background: #ef4444; color: white; width: auto; padding: 8px 16px; }\n.btn-large { font-size: 16px; padding: 15px; margin-top: 15px; }\n.glass-card { background: rgba(255,255,255,0.03); padding: 20px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.05); }\n.settings-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }\n.toggle-label { display: flex; align-items: center; gap: 10px; font-size: 14px; color: #cbd5e1; text-align: left; margin: 10px 0; }\n.toggle-label input { width: auto; margin: 0; }\n.error { color: #f87171; background: rgba(248, 113, 113, 0.1); padding: 10px; border-radius: 6px; }\n.success { color: #34d399; background: rgba(52, 211, 153, 0.1); padding: 10px; border-radius: 6px; }\n.small { font-size: 12px; color: #94a3b8; }\n.hidden { display: none; }\n.status-box { margin-top: 20px; padding: 15px; background: rgba(139, 92, 246, 0.1); border: 1px solid #8b5cf6; border-radius: 8px; color: #c4b5fd; }\na { color: #a78bfa; text-decoration: none; }\na:hover { text-decoration: underline; }\nh1, h2, h3 { color: #f8fafc; margin-top: 0; }\n"""
}

base_dir = r"C:\Users\viy78\Desktop\anime 2.0\anime-video-generator"

for folder in folders:
    os.makedirs(os.path.join(base_dir, folder), exist_ok=True)

for filename, content in files.items():
    with open(os.path.join(base_dir, filename), "w", encoding="utf-8") as f:
        f.write(content)

print("Project scaffold created successfully.")